You are helping me start a brand new project from scratch.

Project context:
- All requirement and planning documents are inside `/docs`
- Frontend work must start in `/frontend`
- The `/frontend` folder currently contains only a fresh initialized React + Vite project, with no real development done yet
- Backend work must be built in `/backend`
- Backend stack: PHP + Slim Framework + Eloquent ORM
- API style: RESTful API
- This is a new project, not a refactor of an old system

Your task:
Read and understand all relevant documents in `/docs`, especially:
- requirements document
- development plan
- database SQL draft

Then start the project in a practical implementation-first way.

Important product context:
This project is a Be Studio membership and class booking platform for a fitness / dance studio.
It includes 3 role-based areas:
1. Student / Member portal
2. Coach portal
3. Admin portal

Core business domains include:
- class browsing
- booking flow
- booking history
- member wallet / balance / frozen balance / expired balance
- membership validity
- coach schedule and student list
- admin dashboard
- member management
- class management
- class execution management
- transaction records
- invoice / receipt
- product sales / counter consumption
- points system
- bilingual support (English + Simplified Chinese)

Critical business rules that must be respected:
1. Booking does NOT immediately deduct money
2. Booking freezes amount first
3. Actual charge happens only when admin starts the class
4. Cancellation before final charge releases the frozen amount back
5. Member and non-member pricing both exist
6. Multi-person booking by head count is supported
7. Walk-in guest flow must be supported
8. If user balance expires, usable balance becomes expired balance and cannot be used
9. If membership expires, member pricing benefit is lost, but historical data remains
10. Points are earned only after actual class charge, not at booking time
11. Admin can convert points into monetary value and recharge back into user balance
12. Product purchases must preserve product, order, item, and transaction records

Technical expectations:
Frontend:
- React
- Vite
- TypeScript
- React Router
- modular folder structure
- role-based layouts
- i18n-ready structure
- reusable UI components
- service/API abstraction layer
- clean, maintainable code

Backend:
- Slim Framework
- Eloquent ORM
- RESTful resource-based API design
- clean module separation
- request validation
- clear controllers/services/actions
- API responses should be consistent and frontend-friendly

Working style:
- Do not jump directly into random coding
- First inspect `/docs`
- Then produce a concise implementation plan based on the docs
- Then create the project foundation and begin actual development
- Make sensible decisions proactively instead of asking too many questions
- Keep code organized and production-oriented
- Prioritize maintainability and clarity

What I want you to do in sequence:

Phase 1 — Understand and align
1. Read the documents in `/docs`
2. Summarize:
   - product modules
   - user roles
   - core business rules
   - MVP scope
3. Identify any ambiguity, inconsistency, or SQL/schema issue
4. If SQL has syntax or structural issues, fix them carefully and explain the fixes

Phase 2 — Define architecture
5. Propose the initial folder structure for both:
   - `/frontend`
   - `/backend`
6. Define route structure for frontend:
   - public/member routes
   - coach routes
   - admin routes
7. Define API module structure for backend
8. Define frontend state/data strategy
9. Define i18n structure for English and Simplified Chinese
10. Define naming conventions and coding conventions to keep the project consistent

Phase 3 — Start implementation
11. Initialize and organize `/frontend` from the fresh React + Vite starter into a real project structure
12. Set up:
   - routing
   - layouts
   - shared UI foundation
   - i18n base
   - API service layer
   - app-level providers if needed
13. Build the first core frontend pages with mock-friendly structure:
   - auth/login entry
   - student home
   - class list / schedule page
   - class detail page
   - booking flow UI
   - account center shell
   - admin layout shell
   - coach layout shell

Phase 4 — Backend foundation
14. Set up `/backend` with Slim + Eloquent project structure
15. Configure database connection and environment setup
16. Create a clean RESTful API skeleton for the core resources
17. Start with the most important backend resources first, such as:
   - auth
   - users
   - member cards
   - courses
   - course sessions
   - bookings
   - transactions
18. Separate controller logic from business logic where appropriate
19. Implement booking-related business logic carefully, especially freeze / charge / cancel release flow

Phase 5 — Documentation during implementation
20. As you work, create or update concise project docs if helpful, such as:
   - `/docs/api-design.md`
   - `/docs/frontend-structure.md`
   - `/docs/backend-structure.md`
   - `/docs/business-rules-notes.md`

Output expectations:
- Show me what you understood from the docs first
- Then show the architecture / structure you want to create
- Then begin making actual changes in `/frontend` and `/backend`
- Explain major decisions briefly
- Keep moving forward instead of stopping too early

Quality bar:
- Code must be readable and maintainable
- Avoid overengineering, but do not make it messy
- Structure the app so formal API integration and future expansion will be easy
- Respect the business rules strictly
- Prefer a clean MVP-first approach

Extra instruction:
If the SQL draft in `/docs` contains errors or ordering issues, do not blindly trust it. Validate it before using it, then produce a corrected version if needed.

Now begin by:
1. reading `/docs`
2. summarizing the project
3. listing risks/issues you found
4. proposing the implementation structure
5. then starting the actual project foundation work